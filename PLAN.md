# 多 Agent 去中心化团队 · 目标拆解与实施计划

> 依据：《深入理解 AI Agent》（bojieli，[在线阅读](https://bojieli.github.io/ai-agent-book/)）
> 核心公式：**Agent = LLM + 上下文 + 工具**（大脑 + 眼睛 + 手脚）
> 版本：v0.1 · 2026-09 · 状态：规划（待确认团队使命后进入执行）
>
> **执行状态（2026-09-18 · Round 14）**：在前一轮基础上完成 **S5 实跑验收**（真实工作区 `teamd` 8090：S1 任务 t-r14-accept → S2 demo 作业 job-000001 五成员 done（`eval/run/2026-09-18-web-demo-02`）→ S3 real:openai-compat 真实 LLM 接入（`/api/jobs` 无 api_key，usage 30/2）→ S4 使命换挡 dry+apply（revision 2，审计后果返回）→ S5 令牌 401/200 + 60/60 并发 + 并发后审计通过；promote 人工门禁闭环发布 r14-accept.md；audit 8/8、probe 3/3）并**据此返工修复两处缺陷**（promote_check 缺 artifact_exists 门禁；mission_switch_dry 沙盒保留 eval/ 消除 evidence_gated 假阳性）。**Round 14 追加**：阅读 [DSH #5180](https://github.com/deepseek-ai/deepseek-harness/discussions/5180) 落地可行实践点——`attention_items()`（需关注聚合，网页「需关注」面板+徽标）、`route_suggest()`（无 Manager 的领域路由建议，网页「路由建议」卡片）、换挡审计后果前置（audit_ok+failing_checks，网页日志标注）；映射表与未落地项见 [docs/web-deployment.md §7](docs/web-deployment.md)。**测试 63/63 OK；audit exit 0；README 已补安装与运行**；已推送 GitHub。**Round 13** 及更早：使命 A（软件开发团队）定稿冻结；M0–M7、P3、D1–D8 全部核验 ✅；M8 已启动（5 成员 + 配额 + 涌现日志）；网页部署 S0→S5 全落地（`web/teamd.py` 纯标准库 HTTP+SSE + `web/index.html` 单文件控制台 + 部署/安全文档）。

---

## 0. 书本纲领速览（计划的坐标轴）

全书十章分两部分：**第一部分「如何构建 Agent」**（Ch1–6：概念/上下文工程/记忆与知识/工具/Coding Agent/交互扩展），**第二部分「如何提升 Agent 能力」**（Ch7–10：评估/模型后训练/持续进化/多 Agent 协作）。

对「多 Agent 去中心化团队」而言，最重要的坐标是：

| 章 | 提供的纲领 | 本项目如何使用 |
|---|---|---|
| Ch1 | Harness 五要素；编排模式（工作流 → 自主）；护栏三层（上下文/执行/数据）；贯穿全书的设计模式（提议者-审核者、渐进式披露、只增不改、边界集+保留集、最小 diff+可回滚） | 团队每个成员的工程宪法；先简单后复杂的原则 |
| Ch2–3 | 上下文工程、KV Cache、Agent Skills、状态栏、压缩；记忆分层、RAG | 成员上下文设计、经验知识库（Ch9 复用） |
| Ch4–5 | 工具分类与设计原则、MCP、主动工具发现；代码是「能创造新工具的工具」→ Agent 自举 | 成员工具集、Harness 层硬约束、未来工具自创 |
| Ch6 | 异步/事件驱动、Computer Use、语音 | 扩展面，非一期范围 |
| Ch7 | 评估：评估环境、指标（Pass@k / Pass^k）、LLM-as-a-Judge、边界集+保留集、可观测性 | 团队的验收标准与持续迭代的仪表 |
| Ch9 | 持续进化：三层轨迹验证（结果/过程/质量）→ 四种更新载体（知识/Prompt+Skill/程序+Harness/参数）→ 双循环（在线执行 / 离线进化）→ 安全边界（证据与指令隔离、待验证区、可信根不可自改） | 团队的「自我进化」组件，M7 |
| **Ch10** | **多 Agent 协作：本书与目标最直接的一章** | 本项目的架构蓝图，详见 §1 |

**Ch10 的核心判断（将作为全项目的第一原则）：**

1. **是否引入新信息**是「多 Agent 是否优于单 Agent」的唯一判据——同一模型自我审查、纯辩论不引入新信息，通常无效；执行反馈、渲染截图、外部工具验证引入新信息，显著有效。
2. 两个设计维度：**上下文共享 vs 隔离**；**协作拓扑：对等 / 管理者 / 去中心化**。
3. 通信机制 = 进程间通信两大范式：共享内存（=共享文件系统）/ 消息传递（=工具参数、消息总线）。
4. 去中心化（Decentralized）＝没有运行时中心控制者；代表：MetaGPT（SOP+消息池+订阅，控制流实为流水线，伪去中心化）、AutoGen 群聊（共享对话+中心化选人，混合）、**OpenAI Swarm（控制流真对等、handoff 接力、需环检测/预算/访问链保护）**。
5. 基础设施：虚拟文件系统四区域（私有工作区/多 Agent 共享区/外部挂载/系统内置资源）；控制平面（消息信封、状态查询、优雅/级联终止、资源调度）。
6. 失败模式六类：并发冲突（乐观锁/worktree）、错误级联（交叉验证）、同质趋同（差异化+命名空间+配额）、互相扯皮（目标优先级+所有权+人工仲裁）、循环失控（预算+独立 key）、理解债（人不能外包理解）。
7. 成本：多 Agent 收益必须覆盖数倍 token 开销；步骤预算要有「预算感知」。

---

## 1. 最终目标（Goal Statement）

**在 DeepSeek Harness（DSH）运行时之上，构建一支多 Agent 去中心化团队**：没有中心化的 Manager 节点，若干职责对等的成员 Agent 各自持有独立上下文与工具，通过「共享文件系统（数据平面）+ 消息总线/交接协议（控制平面）」自主通信、分工与相互验证，能够协作完成真实任务；并具备书本要求的四项工程属性：**可评估、可容错、可进化、成本可控**。

### 1.1 成功判据（对齐书本，全部可验证）

| # | 判据 | 验证方式 |
|---|---|---|
| D1 | 无中心控制者：任务路由/分工决策由成员自主做出，无固定 Manager 节点 | 架构审查：任一成员崩溃不导致系统停摆（对照试验） |
| D2 | 真并行：成员上下文独立、可并发执行、互不阻塞 | 实验中记录重叠执行时间段与独立轨迹 |
| D3 | 信息增量：每次多 Agent 交互都有可指认的新信息（执行结果/视觉/工具反馈） | 每次协作决策记录「新增信息」字段；无信息增量的协作被拒绝 |
| D4 | 接口清晰：结构化移交包 + 消息信封 + 文件引用（路径而非内容） | 协议 schema 检查 + 消息日志审计 |
| D5 | 容错闭环：环检测、预算、取消级联、交叉验证、冲突治理 | 故障注入测试（制造环、超预算、并发写冲突） |
| D6 | 可评估：评估集、指标、LLM-as-Judge、边界集+保留集 | M6 评估管线输出报告 |
| D7 | 可进化：经验经三层验证 → 知识/Skill/程序 更新，可回滚 | M7 进化闭环实验（改进前后对比） |
| D8 | 成本可控：token 预算、并发上限、预算感知 | 每次运行的成本记录 |

### 1.2 目标拆解（WBS，按书本章节组织）

```
G0 多 Agent 去中心化团队
├── M0 环境与协议基线
│   ├── 0.1 运行时映射 (DSH = Agent 运行时/内核；双方角色的语义约定)
│   ├── 0.2 协议规范：消息信封(Envelope) JSON Schema、Handoff 包 Schema、Agent Card
│   ├── 0.3 工作区骨架：目录树 + 四区域布局
│   └── 0.4 设计规范：接口、命名空间、权限、验收标准（防「接口不清」这类系统设计缺陷）
├── M1 单 Agent 内核（Ch1/2/4/5 子集）—— 先会跑，再谈团队
│   ├── 1.1 成员 Agent 原型：系统提示词 + 上下文管理 + 核心工具集
│   ├── 1.2 ReAct 循环 + 停止条件 + 状态栏/进度文件（Ch2）
│   ├── 1.3 Skills 按需加载（渐进式披露）
│   └── 1.4 验证：单 Agent 完成 pilot 任务，记录轨迹（JSONL）
├── M2 角色化成员（Ch1 角色切换 + Ch10 上下文隔离）
│   ├── 2.1 角色集设计（依据团队使命；每个角色=独立 preset + 专属工具子集 + 专属技能）
│   ├── 2.2 私有工作区(scratchpad)与共享区边界
│   └── 2.3 权限/工具隔离：权限级差异走 Harness 门，风格级差异走 Skill（书 10.2 决策表）
├── M3 协作基础设施（Ch10 数据平面 + 控制平面）
│   ├── 3.1 虚拟文件系统四区域（scratch / shared / mounts / skills）
│   ├── 3.2 消息总线（总线=带信封的 JSONL 文件 + 订阅规则；异步、广播、路由）
│   ├── 3.3 控制平面原语：spawn / send / status(进度文件+轨迹持久化) / terminate(优雅+级联) / 发现
│   ├── 3.4 资源与调度：每任务 token/步数预算、并发上限、抢占
│   └── 3.5 移交互{0}协{1}议：Handoff 包（含 visited_agents、remaining_budget、环检测）
├── M4 去中心化协作模式（Ch10 核心）
│   ├── 4.1 对等形态：提议者-审核者（含独立证据）、辩论、专家小组
│   ├── 4.2 消息池+订阅形态（MetaGPT 机制，SOP 流水线；作为可控基线）
│   ├── 4.3 真去中心化形态：handoff 网络（Swarm 式，无 Manager）
│   └── 4.4 三形态对照实验：同一任务，比收敛性、成本、质量、容错
├── M5 失败模式治理（Ch10 六类失败 → 六道防线）
│   ├── 5.1 并发冲突：文件版本号/乐观锁 + worktree 隔离
│   ├── 5.2 错误级联：独立交叉验证（只读原始证据复审，不读前序思考）
│   ├── 5.3 同质趋同：模型/上下文/信息源差异化 + 各成员命名空间 + 配额
│   ├── 5.4 互相扯皮：目标优先级、资源所有权、可验证冲突规则 + 仲裁入口（人/规则，非 Manager）
│   ├── 5.5 循环失控：预算上限 + 独立凭证 + 环检测
│   └── 5.6 理解债：全链路可观测（轨迹/进度/决策理由）+ 人工检查点
├── M6 评估体系（Ch7，最小化先行，不做完不扩编）
│   ├── 6.1 评估任务集（边界集 + 保留集 + 安全集）
│   ├── 6.2 指标：成功率/部分成功/失败、成本、轮数、质量 Rubric
│   ├── 6.3 LLM-as-a-Judge（带证据的标尺，低置信度拒评）
│   └── 6.4 可观测性：轨迹、消息、成本、失败归因
├── M7 持续进化（Ch9 —— 团队「长本事」的引擎）
│   ├── 7.1 三层轨迹验证器（结果/过程/质量）
│   ├── 7.2 经验知识库：不可变轨迹 → 单次分析 → 跨轨迹归纳 → 正式文档
│   ├── 7.3 指令载体：Prompt/Skill 最小 diff 更新（边界集+保留集回归）
│   ├── 7.4 程序载体：失败轨迹触发重试/熔断/工具等 Harness 补丁（最小补丁+回滚）
│   └── 7.5 安全边界：证据与指令隔离、待验证区、可信根不可自改、人工门禁
├── M8 规模化与涌现（长期/可选，Ch10「Agent 社会」）
│   ├── 8.1 记忆流+反思+规划（Smallville 机制）
│   ├── 8.2 市场机制协作（Pinchwork 式任务市集，价格信号替代中心调度）
│   ├── 8.3 策略博弈 / 长周期模拟（Agentopia 式周制 + 生活奖励）
│   └── 8.0 本项目起点（Round 11 已启动）：5 成员就位（+architect/ops）；资源配额
│       （budget pool `quota_*` + 并发上限）+ 每步 token 用量登记；待办：长周期/多步任务
│       + 配额实测 + 协作涌现日志（Round 12+）
└── M9 越级项（不达 D3 判据不启用）：Manager 模式仅作为对照基线保留，不作为目标架构
```

### 1.3 届次路线图（Roadmap · 每轮一个可交付增量）

| 阶段 | 内容 | 完成标志（DONE） | 对应书本 |
|---|---|---|---|
| **P0 规划** | 本书阅读、目标拆解、计划 | 本文件获批；团队使命确定 | 引言/Ch10 |
| **P1 基线** | M0 + M1 | 单 Agent 在 pilot 任务上可复现达标；协议 v1 冻结 | Ch1/2/4 |
| **P2 双人** | M2 + 部分 M3 | 两个角色成员完成一次「提议-审核」协作交付（含证据） | Ch5/10 |
| **P3 团队** | M3 完成 + M4.2 消息池基线 | 3–5 成员经消息池+订阅完成一个多步任务，无 Manager | Ch10 |
| **P4 去中心化** | M4.3 + M5 | 同一任务在 handoff 网络下完成；六类故障注入全部通过 | Ch10 |
| **P5 度量** | M6 | 评估报告：成功率/成本/质量，边界+保留集稳定 | Ch7 |
| **P6 进化** | M7 | 一次真实进化：经验→最小补丁→回归通过→灰度；回滚演练 | Ch9 |
| **P7 深化** | M8（可选） | 规模/市场/长周期实验 | Ch10 社会 |

### 1.4 每阶段通用「验收闸门」（源自 Ch1 设计模式）

- **提议者-审核者**：任何产物发布前，由独立审核者基于**独立证据**（执行/渲染/工具验证）放行；模型不得自我批准完成。
- **边界集 + 保留集**：每次改动在「触发问题的边界案例」上必须改善，在「正常任务保留集」上不得退化。
- **最小 diff + 可回滚**：一切能力更新（Skill、工具、Harness 代码）以最小补丁形式提交并保留回滚点。
- **只增不改**：轨迹、消息、决策理由只追加；可变部分（索引/总结/经验）周期性离线重建。
- **渐进式披露**：成员上下文只加载索引/目录，细节按需读取。

---

## 2. 运行时映射（决定实现语言，不猜）

| 书本概念 | 本项目落地 |
|---|---|
| Agent 运行时 / 内核 | DeepSeek Harness（DSH）；宿主组合=共享注册表；成员=agent preset |
| 一个成员 Agent | `~/.dsh/.agent-presets/<role>/` 中的 preset（系统提示词+工具子集+Skill），或动态 Cordis 插件 |
| 成员私有上下文 | preset 的会话上下文 + 私有 scratchpad 目录 |
| 共享文件系统（数据平面） | 本工作区：`shared/`（产物、交接包）；`system/`（消息、状态、日志） |
| 消息总线（控制平面） | `system/messages/*.jsonl` 信封文件 + 订阅表（`subscriptions.json`） |
| spawn/list/status/cancel | DSH 子 Agent 注册表 / `list_agents` / `send_message` / `interrupt_agent` |
| 工具 | bash/read/write/grep/glob 等现有工具 + 按角色定制的插件工具 |
| 评估与 LLM-as-Judge | M6 评估脚本 + Judge 工具（独立上下文/独立凭证） |
| 持续进化 | 睡眠学习任务（离线）+ 最小补丁 → 新插件/新 Skill → 验证 → 升级 |

> 边界纪律（照书本与 DSH 规则）：宿主组合/注册表是共享设施，改动走主机组合；**每用户 preset 只放本会话贡献**；绝不修改部署自带的 shipped preset 安装。

---

## 3. 关键风险与对策（先写进计划再动手）

| 风险 | 书本依据 | 对策 |
|---|---|---|
| 为了多而多，实际单 Agent 更好 | Ch10「新信息判据」 | 每个新增成员/协作点必须回答「引入什么新信息」；答不出则不加 |
| 去中心化不收敛（环、扯皮） | Ch10 失败模式五/四 | handoff 带 `visited_agents` + 预算衰减；冲突走可验证规则，规则不可解→人工仲裁 |
| 上下文膨胀/压缩失效 | Ch10 / Ch2 | 交接只传「任务描述+已确认事实+产物引用」，不传思考过程；成员上下文互相隔离 |
| 成本失控 | Ch10 | 预算感知：预算感知调度、并行上限、机械任务用小模型/程序化 |
| 提示注入污染经验 | Ch9 安全边界 | 网页/工具输出=不可信证据，与指令隔离；固定 schema 提取+来源保留 |
| 进化自证成功 | Ch9 | 可信根（验证器/测试/门槛）与更新者分离，更新者无权修改 |
| 理解债 | Ch10 失败模式六 | 全链路可观测 + 交付件审计 + 人保留技术判断（评审点） |
| 同质趋同 | Ch10 失败模式三 | 成员之间模型/上下文/信息源/提示词制造差异；命名空间；配额 |

---

## 4. 下一步（本计划获批后立即执行）

1. **P0 收尾**：确认团队使命（见待确认问题）→ 角色集草案 v1。
2. **P1 开工**：初始化工作区骨架（四区域目录 + 协议 schema + 单成员 preset 原型）。
3. **P1 验证**：单 Agent 跑通 pilot 任务，产出首条 JSONL 轨迹 → 进入 P2 双人协作（提议者-审核者）。
